# Исправление белого экрана при открытии сайтов

## Проблема

При открытии любого сайта (Google, GitHub и т.д.) отображается только белый экран.

## Причина

Проект был мигрирован с **Electron на Tauri**, но компонент `WebViewContainer.tsx` продолжал использовать Electron-специфичный тег `<webview>`, который **не поддерживается в Tauri**.

В Tauri нет аналога Electron `<webview>` тега. Это критическая разница между двумя фреймворками.

## Решение

Заменили `<webview>` на стандартный HTML `<iframe>` с настройками безопасности.

### Что было изменено:

1. **WebViewContainer.tsx**
   - Заменен `<webview>` на `<iframe>`
   - Удалены Electron-специфичные события (did-navigate, did-start-loading и т.д.)
   - Упрощена обработка загрузки страниц
   - Удалены недоступные API (findInPage, context-menu и т.д.)

2. **App.tsx**
   - Обновлены функции навигации (goBack, goForward, reload)
   - Изменена реализация zoom через CSS transform
   - Упрощены DevTools и Print функции

## Ограничения iframe

⚠️ **Важно:** iframe имеет существенные ограничения по сравнению с Electron webview:

### Не работает:
- ❌ История навигации (Back/Forward) для внешних сайтов
- ❌ Получение URL и заголовка страницы для внешних сайтов
- ❌ Favicon (нужно использовать внешние API)
- ❌ Поиск на странице (Find in Page)
- ❌ DevTools для конкретного iframe
- ❌ Контекстное меню с параметрами

### Работает:
- ✅ Базовая загрузка страниц
- ✅ Безопасность через sandbox
- ✅ Zoom через CSS (не идеально, но работает)
- ✅ Базовые события (load, error)

### Сайты которые могут НЕ работать:
- ❌ Google (блокирует загрузку в iframe)
- ❌ Facebook (блокирует загрузку в iframe)
- ❌ Twitter/X (блокирует загрузку в iframe)

Эти сайты используют заголовок `X-Frame-Options: DENY` или `SAMEORIGIN`, который запрещает загрузку в iframe.

## Запуск

После исправления запустите приложение:

```bash
npm run dev
```

Теперь сайты должны загружаться, но с ограничениями описанными выше.

## Альтернативные решения

Для полноценного браузера на Tauri рекомендуется:

1. **Использовать отдельные окна Tauri** для каждой вкладки
   - Полный контроль над навигацией
   - Доступ к DevTools
   - Но сложнее управление

2. **Разработать Tauri плагин** для WebView
   - Ближе к Electron webview
   - Требует знания Rust

3. **Вернуться к Electron** если функциональность критична
   - Все работает как раньше
   - Но больше размер и потребление памяти

## Дополнительная информация

Подробное описание ограничений и альтернативных решений см. в [IFRAME_LIMITATIONS.md](./IFRAME_LIMITATIONS.md)

## Статус

✅ **Исправлено:** Белый экран больше не появляется  
⚠️ **Ограничения:** Некоторые функции недоступны из-за ограничений iframe  
🔄 **В разработке:** Поиск лучших решений для полноценного браузера на Tauri
