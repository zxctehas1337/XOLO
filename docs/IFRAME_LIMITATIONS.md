# Ограничения iframe в Tauri

## Проблема

При миграции с Electron на Tauri была обнаружена критическая проблема: **Tauri не поддерживает Electron `<webview>` тег**.

В Electron `<webview>` предоставляет изолированный контекст для отображения внешних веб-страниц с полным контролем (навигация, zoom, DevTools и т.д.). В Tauri такого функционала нет.

## Решение

Заменили `<webview>` на стандартный HTML `<iframe>` с максимальными разрешениями sandbox.

## Ограничения iframe

### ❌ Что НЕ работает:

1. **Навигация (Back/Forward)**
   - `iframe.contentWindow.history.back()` работает только для same-origin
   - Для cross-origin сайтов (Google, GitHub и т.д.) это заблокировано браузером
   - **Решение**: Нужно реализовать собственную систему истории навигации

2. **Получение URL**
   - `iframe.contentWindow.location.href` работает только для same-origin
   - Для cross-origin сайтов выбрасывается SecurityError
   - **Решение**: Отслеживать URL на уровне приложения

3. **Получение заголовка страницы**
   - `iframe.contentDocument.title` работает только для same-origin
   - **Решение**: Использовать API для получения метаданных или парсить HTML

4. **Favicon**
   - Нет доступа к favicon через iframe API
   - **Решение**: Использовать внешние сервисы (Google Favicon API, DuckDuckGo Icons)

5. **Zoom**
   - Нет `setZoomFactor()` как в Electron webview
   - **Решение**: Использовать CSS `transform: scale()` (работает, но не идеально)

6. **DevTools**
   - Нет `openDevTools()` для конкретного iframe
   - **Решение**: Можно открыть DevTools для всего окна (F12)

7. **Поиск на странице (Find in Page)**
   - Нет `findInPage()` API
   - **Решение**: Можно использовать `window.find()` через postMessage, но это ограничено

8. **Контекстное меню**
   - Нет событий `context-menu` с параметрами (linkURL, selectionText)
   - **Решение**: Реализовать собственное контекстное меню на уровне приложения

9. **События навигации**
   - Нет событий `did-navigate`, `did-start-loading`, `did-stop-loading`
   - Есть только базовые `load` и `error`
   - **Решение**: Отслеживать состояние загрузки вручную

10. **Новые окна**
    - Нет события `new-window` для перехвата открытия новых окон
    - **Решение**: Настроить `sandbox` атрибут для контроля popups

### ✅ Что работает:

1. **Базовая загрузка страниц**
   - Можно загружать любые внешние сайты через `src` атрибут

2. **Sandbox безопасность**
   - Можно настроить разрешения через `sandbox` атрибут
   - Можно контролировать доступ к API через `allow` атрибут

3. **CSS стилизация**
   - Можно стилизовать сам iframe (размер, границы и т.д.)

4. **Базовые события**
   - `load` - страница загружена
   - `error` - ошибка загрузки

## Альтернативные решения

### 1. Использовать отдельные окна Tauri

Вместо iframe создавать отдельные окна через Tauri API для каждой вкладки.

**Плюсы:**
- Полный контроль над навигацией
- Доступ к DevTools
- Лучшая изоляция

**Минусы:**
- Сложнее управление вкладками
- Больше потребление ресурсов
- Нужно переписывать архитектуру

### 2. Использовать Tauri WebView плагин

Если существует плагин для создания WebView компонентов.

**Плюсы:**
- Ближе к Electron webview
- Лучший контроль

**Минусы:**
- Может не существовать или быть нестабильным
- Дополнительные зависимости

### 3. Гибридный подход

Использовать iframe для простых случаев и отдельные окна для сложных.

**Плюсы:**
- Баланс между простотой и функциональностью

**Минусы:**
- Сложность реализации
- Непоследовательный UX

## Рекомендации

### Краткосрочные (текущее решение):

1. ✅ Использовать iframe с максимальными разрешениями
2. ✅ Реализовать собственную систему истории навигации
3. ✅ Использовать CSS transform для zoom
4. ✅ Отслеживать URL на уровне приложения
5. ✅ Использовать внешние API для favicon

### Долгосрочные:

1. 🔄 Исследовать возможность создания Tauri плагина для WebView
2. 🔄 Рассмотреть архитектуру с отдельными окнами
3. 🔄 Реализовать proxy для обхода CORS ограничений
4. 🔄 Добавить серверный компонент для получения метаданных страниц

## Известные проблемы

### Проблема: Белый экран при открытии сайтов

**Причина:** Использование Electron `<webview>` тега в Tauri приложении

**Решение:** Заменено на `<iframe>` (текущий коммит)

### Проблема: CORS ошибки

**Причина:** Многие сайты блокируют загрузку в iframe через `X-Frame-Options` или CSP

**Решение:** 
- Некоторые сайты (Google, GitHub) могут не загружаться в iframe
- Нужно показывать понятное сообщение об ошибке
- Рассмотреть использование отдельных окон для таких сайтов

### Проблема: Нет истории навигации

**Причина:** iframe не предоставляет доступ к истории cross-origin страниц

**Решение:**
- Реализовать собственный стек истории на уровне приложения
- Сохранять URL при каждой навигации
- Реализовать кнопки Back/Forward через этот стек

## Тестирование

### Сайты которые должны работать:
- ✅ Wikipedia
- ✅ Stack Overflow
- ✅ Reddit
- ✅ YouTube (с ограничениями)

### Сайты которые могут НЕ работать:
- ❌ Google (X-Frame-Options: SAMEORIGIN)
- ❌ Facebook (X-Frame-Options: DENY)
- ❌ Twitter/X (X-Frame-Options: SAMEORIGIN)
- ❌ GitHub (может блокировать)

## Выводы

Миграция с Electron на Tauri требует компромиссов. `<iframe>` - это временное решение с существенными ограничениями. Для полноценного браузера рекомендуется:

1. Использовать отдельные окна Tauri для каждой вкладки
2. Или разработать собственный Tauri плагин для WebView
3. Или вернуться к Electron если функциональность критична

**Текущее состояние:** Базовая функциональность работает, но с ограничениями. Подходит для MVP и тестирования концепции.
