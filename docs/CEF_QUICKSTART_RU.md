# Быстрый старт с CEF

## Что изменилось?

XOLO Browser теперь использует **Chromium Embedded Framework (CEF)** вместо iframe/WebView. Это означает:

✅ **Google сервисы работают!** Gmail, YouTube, Google Docs - всё работает как в обычном Chrome  
✅ **Лучшая производительность** - полноценный движок Chromium  
✅ **Современные веб-стандарты** - поддержка всех новых API  
✅ **Нет ограничений** - никаких проблем с same-origin policy  

## Установка

### 1. Подготовка CEF

```bash
# Запустите скрипт настройки CEF
./setup-cef.sh
```

Этот скрипт скопирует CEF библиотеки в нужное место.

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск

```bash
# Режим разработки
npm run tauri dev

# Сборка для продакшена
npm run tauri build
```

## Первый запуск

При первом запуске CEF инициализируется, это может занять несколько секунд. Вы увидите:

```
Инициализация браузера...
```

После инициализации браузер готов к работе!

## Проверка работы Google сервисов

1. Откройте новую вкладку
2. Перейдите на `https://gmail.com`
3. Войдите в свой аккаунт Google
4. Всё должно работать как в обычном Chrome!

## Что делать если что-то не работает?

### CEF не инициализируется

```bash
# Проверьте наличие библиотек
ls -la src-tauri/target/release/cef/

# Если их нет, запустите setup-cef.sh снова
./setup-cef.sh
```

### Ошибки при сборке

```bash
# Очистите кэш и пересоберите
cd src-tauri
cargo clean
cd ..
npm run tauri build
```

### Страницы не загружаются

1. Откройте DevTools (F12)
2. Проверьте консоль на ошибки
3. Попробуйте открыть remote debugging: `http://localhost:9222`

## Отличия от старой версии

### Что изменилось в коде?

**Было (WebView):**
```tsx
<WebViewContainer
  tab={tab}
  isActive={isActive}
  onUpdate={onUpdate}
/>
```

**Стало (CEF):**
```tsx
<CefViewContainer
  tab={tab}
  isActive={isActive}
  onUpdate={onUpdate}
/>
```

### API изменения

**Было:**
```typescript
// iframe API
iframe.contentWindow.location.href = url;
```

**Стало:**
```typescript
// CEF API
await CefAPI.navigate(tabId, url);
```

## Производительность

CEF использует больше памяти чем iframe, но работает быстрее:

- **iframe**: ~50MB на вкладку
- **CEF**: ~100MB на вкладку

Для экономии памяти браузер автоматически замораживает неактивные вкладки.

## Размер приложения

- **Без CEF**: ~10MB
- **С CEF**: ~110MB

Это нормально для полноценного браузера на базе Chromium.

## Следующие шаги

1. Прочитайте [полную документацию](./CEF_INTEGRATION_RU.md)
2. Изучите [примеры использования CEF API](../src/renderer/cef-api.ts)
3. Посмотрите [реализацию CefViewContainer](../src/renderer/components/CefViewContainer.tsx)

## Поддержка

Если у вас возникли проблемы:

1. Проверьте [FAQ](./FAQ_TAURI.md)
2. Откройте issue на GitHub
3. Напишите в Telegram чат проекта

---

**Приятного использования XOLO Browser с CEF! 🚀**
