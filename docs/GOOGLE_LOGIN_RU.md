# 🔐 Решение проблемы входа в Google сервисы

## Проблема
Google блокирует вход в Gmail, YouTube и другие сервисы из браузеров на базе Electron с ошибкой "небезопасный браузер".

## ✅ Решение реализовано

Браузер XOLO теперь **автоматически** открывает страницы входа Google в вашем системном браузере (Chrome, Firefox и т.д.).

### Как это работает:

1. **Попытка входа** - Вы пытаетесь войти в Gmail, YouTube или любой Google сервис
2. **Автоматический перехват** - XOLO определяет страницу входа Google
3. **Открытие в системном браузере** - Страница авторизации открывается в вашем основном браузере
4. **Уведомление** - Вы видите сообщение: "Вход в Google открыт в системном браузере для безопасности"
5. **Вход** - Войдите в свой аккаунт в системном браузере
6. **Готово** - Вернитесь в XOLO и продолжайте работу

### Что перехватывается:

- ✅ `accounts.google.com/signin` - Страница входа
- ✅ `accounts.google.com/ServiceLogin` - Альтернативная страница входа
- ✅ `accounts.google.com/v3/signin` - Новая версия входа
- ✅ `accounts.google.com/AccountChooser` - Выбор аккаунта
- ✅ `accounts.google.com/o/oauth2` - OAuth авторизация
- ✅ `accounts.google.com/embedded/setup` - Встроенная настройка

### Поддерживаемые сервисы:

- 📧 Gmail
- 🎥 YouTube
- 📁 Google Drive
- 📄 Google Docs/Sheets/Slides
- 📅 Google Calendar
- 📷 Google Photos
- 🔍 Google Search (с входом)
- И все остальные Google сервисы!

## 🔧 Техническая реализация

### 1. Перехват навигации (main.ts)
```typescript
contents.on('will-navigate', (event, url) => {
  const isGoogleAuth = url.includes('accounts.google.com/signin') || 
                       url.includes('accounts.google.com/ServiceLogin') ||
                       // ... другие URL
  
  if (isGoogleAuth) {
    event.preventDefault();
    shell.openExternal(url); // Открываем в системном браузере
    
    // Показываем уведомление
    mainWindow?.webContents.send('google-auth-external', {
      message: 'Вход в Google открыт в системном браузере для безопасности',
      url: url
    });
  }
});
```

### 2. Перехват popup окон
```typescript
contents.setWindowOpenHandler(({ url }) => {
  const isGoogleAuth = url.includes('accounts.google.com');
  
  if (isGoogleAuth) {
    shell.openExternal(url);
    return { action: 'deny' };
  }
  
  return { action: 'deny' };
});
```

### 3. Toast уведомления (Toast.tsx)
Красивое уведомление в правом нижнем углу информирует пользователя о том, что вход открыт в системном браузере.

### 4. Информация в настройках
В разделе "Аккаунт" → "Google аккаунт" добавлено информационное сообщение, объясняющее работу функции.

## 🎯 Преимущества решения

1. **Безопасность** - Google доверяет вашему системному браузеру
2. **Простота** - Работает автоматически, без настройки
3. **Прозрачность** - Пользователь видит уведомление и понимает, что происходит
4. **Надежность** - Не требует обхода защиты Google
5. **Совместимость** - Работает со всеми Google сервисами

## 🚀 Альтернатива: OAuth в настройках

Для полной интеграции можно использовать OAuth:

1. Откройте **Настройки** (Ctrl+J)
2. Раздел **"Аккаунт"** → **"Google аккаунт"**
3. Нажмите **"Войти через Google"**
4. Авторизация откроется в системном браузере
5. После входа аккаунт синхронизируется с браузером

OAuth использует локальный сервер на порту 8888 для получения токена авторизации.

## 📝 Примечания

- Решение работает для **всех** Google сервисов
- Не требует изменения User-Agent или других хаков
- Соответствует политике безопасности Google
- Пользователь всегда информирован о происходящем

## 🐛 Если что-то не работает

1. Убедитесь, что у вас установлен системный браузер (Chrome, Firefox, Edge)
2. Проверьте, что браузер может открывать внешние ссылки
3. Посмотрите консоль разработчика (F12) на наличие ошибок
4. Создайте issue на GitHub с описанием проблемы
