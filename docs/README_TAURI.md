# XOLO Browser - Tauri Edition

Современный веб-браузер, построенный на **Tauri + Rust + React + TypeScript**.

## 🚀 Почему Tauri?

Мы мигрировали с Electron на Tauri и получили:

- **30x меньше размер** приложения (5 MB vs 150 MB)
- **5x меньше потребление** памяти (50-100 MB vs 300-500 MB)
- **3x быстрее запуск** (0.5-1 сек vs 2-3 сек)
- **Лучшая безопасность** благодаря Rust
- **Более нативное** приложение

## ✨ Возможности

### Основные
- 🌐 Полноценный веб-браузер с поддержкой современных веб-стандартов
- 📑 Неограниченное количество вкладок
- 🗂️ Workspaces для организации вкладок
- 🔖 Закладки с импортом/экспортом
- 📜 История посещений
- ⬇️ Менеджер загрузок
- 🔍 Поиск по вкладкам (Ctrl+Shift+A)

### Продвинутые
- 🎨 Настраиваемые темы и цвета
- 🖼️ Пользовательские обои
- ❄️ Автоматическая заморозка неактивных вкладок для экономии памяти
- 💾 Автосохранение и восстановление сессий
- 🔐 Google OAuth интеграция
- 🚫 Встроенный блокировщик рекламы
- ⌨️ Полная поддержка горячих клавиш
- 📥 Импорт данных из Chrome/Edge

## 🛠️ Технологии

### Бэкенд
- **Rust** - системный язык программирования
- **Tauri** - фреймворк для создания десктопных приложений
- **tokio** - асинхронный runtime
- **reqwest** - HTTP клиент
- **serde** - сериализация/десериализация

### Фронтенд
- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev сервер
- **CSS3** - стилизация

## 📦 Установка

### Требования

1. **Rust** (1.70+)
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

2. **Node.js** (18+)
```bash
# Установите через nvm, apt, или скачайте с nodejs.org
```

3. **Системные зависимости**

#### Linux (Debian/Ubuntu)
```bash
sudo apt update
sudo apt install libwebkit2gtk-4.0-dev \
    build-essential \
    curl \
    wget \
    file \
    libssl-dev \
    libgtk-3-dev \
    libayatana-appindicator3-dev \
    librsvg2-dev
```

#### Linux (Fedora)
```bash
sudo dnf install webkit2gtk4.0-devel \
    openssl-devel \
    curl \
    wget \
    file \
    libappindicator-gtk3-devel \
    librsvg2-devel
```

#### macOS
```bash
xcode-select --install
```

#### Windows
Установите [Microsoft Visual Studio C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)

### Установка зависимостей

```bash
npm install
```

### Генерация иконок

```bash
npm install -g @tauri-apps/cli
cd src-tauri
tauri icon ../public/icon.png
cd ..
```

## 🚀 Запуск

### Режим разработки
```bash
npm run dev
```

Приложение откроется автоматически с hot-reload.

### Сборка production
```bash
npm run build
```

Готовые бинарники будут в `src-tauri/target/release/bundle/`

## ⌨️ Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl+T` | Новая вкладка |
| `Ctrl+W` | Закрыть вкладку |
| `Ctrl+Shift+T` | Восстановить закрытую вкладку |
| `Ctrl+L` | Фокус на адресной строке |
| `Ctrl+R` / `F5` | Перезагрузить страницу |
| `Ctrl+F` | Поиск на странице |
| `Ctrl+D` | Добавить в закладки |
| `Ctrl+H` | История |
| `Ctrl+J` | Загрузки |
| `Ctrl+Shift+A` | Поиск по вкладкам |
| `Ctrl++` | Увеличить масштаб |
| `Ctrl+-` | Уменьшить масштаб |
| `Ctrl+0` | Сбросить масштаб |
| `F11` | Полноэкранный режим |
| `F12` / `Ctrl+Shift+I` | DevTools |
| `Ctrl+P` | Печать |
| `Alt+Home` | Домашняя страница |

## 📁 Структура проекта

```
xolo-browser/
├── src-tauri/              # Rust бэкенд
│   ├── src/
│   │   ├── main.rs         # Точка входа, горячие клавиши
│   │   ├── commands.rs     # IPC команды
│   │   ├── storage.rs      # Хранилище данных
│   │   ├── oauth.rs        # Google OAuth
│   │   └── downloads.rs    # Управление загрузками
│   ├── Cargo.toml          # Rust зависимости
│   ├── tauri.conf.json     # Конфигурация Tauri
│   └── icons/              # Иконки приложения
├── src/renderer/           # React фронтенд
│   ├── components/         # React компоненты
│   ├── styles/             # CSS стили
│   ├── types/              # TypeScript типы
│   ├── tauri-api.ts        # Адаптер Tauri API
│   ├── App.tsx             # Главный компонент
│   └── main.tsx            # Точка входа
├── public/                 # Статические файлы
├── package.json            # Node.js зависимости
└── vite.config.ts          # Конфигурация Vite
```

## 🔧 Конфигурация

### Настройки приложения

Настройки хранятся в:
- **Linux**: `~/.local/share/xolo-browser/settings.json`
- **macOS**: `~/Library/Application Support/xolo-browser/settings.json`
- **Windows**: `%APPDATA%\xolo-browser\settings.json`

### Доступные настройки

```json
{
  "searchEngine": "google",
  "theme": "dark",
  "accentColor": "#7c3aed",
  "fontSize": 14,
  "tabPosition": "top",
  "showBookmarksBar": true,
  "adBlockEnabled": true,
  "wallpaperUrl": "/walpaper1.jpg"
}
```

## 🐛 Отладка

### Rust логи
```bash
RUST_LOG=debug npm run dev
```

### DevTools
Нажмите `F12` или `Ctrl+Shift+I` в приложении

### Проверка версий
```bash
rustc --version
node --version
npm --version
```

## 📊 Производительность

### Метрики (Linux x64)

| Метрика | Значение |
|---------|----------|
| Размер приложения | ~5 MB |
| Потребление RAM (idle) | ~50 MB |
| Потребление RAM (10 вкладок) | ~100 MB |
| Время запуска | ~0.5 сек |
| Размер установщика | ~3 MB |

## 🔒 Безопасность

- ✅ Memory safety благодаря Rust
- ✅ Allowlist API (только разрешенные команды)
- ✅ Context isolation
- ✅ Нет Node.js runtime в production
- ✅ Системный WebView (обновляется с ОС)

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста:

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 🙏 Благодарности

- [Tauri](https://tauri.app/) - за отличный фреймворк
- [React](https://react.dev/) - за UI библиотеку
- [Rust](https://www.rust-lang.org/) - за безопасный системный язык

## 📚 Дополнительная документация

- [Быстрый старт](./QUICKSTART_TAURI.md)
- [Руководство по миграции](./MIGRATION_TAURI.md)
- [Сравнение с Electron](./COMPARISON.md)
- [FAQ](./FAQ_TAURI.md)
- [Чеклист миграции](./MIGRATION_CHECKLIST.md)

## 🔗 Полезные ссылки

- [Документация Tauri](https://tauri.app/v1/guides/)
- [Rust Book](https://doc.rust-lang.org/book/)
- [React документация](https://react.dev/)
- [TypeScript документация](https://www.typescriptlang.org/docs/)

## 💬 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [FAQ](./FAQ_TAURI.md)
2. Посмотрите [Issues](https://github.com/your-username/xolo-browser/issues)
3. Создайте новый Issue с подробным описанием

---

Сделано с ❤️ и Rust 🦀
